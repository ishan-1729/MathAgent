# Methods

Each file in this directory describes a reusable proof technique. Method files should be concise enough to load into agent context and structured enough to guide search.

Use `TEMPLATE.md` for new methods.
