# Instructions

This directory contains global rules for MathAgent proof attempts, contamination control, Lean output, and disallowed final methods.

Problem-specific instructions may add constraints, but should not silently weaken the global elementary-proof rule.
