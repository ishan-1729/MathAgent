# Lean Output Protocol

Lean files are not required for every early attempt.

When Lean is included, separate theorem statement, helper lemmas, and proof. Keep helper lemmas small and name them according to the informal proof step they support.

Avoid large `sorry` blocks in final checked attempts. During exploration, placeholders are allowed only when their scope is recorded.

Track percentage compilable and record where compilation first fails.
