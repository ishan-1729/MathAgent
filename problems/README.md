# Problems

Each problem gets its own folder.

Default contents are:

- `problem.md`
- `allowed_inputs.md`
- `attempts/`
- `lean/`
- `eval/`

Do not include a standard proof unless the folder is marked as intentionally seeded.
