# AGENTS.md

MathAgent keeps a **single source of agent instructions** for every tool — Claude Code
(Claude Fable 5) and Codex (GPT 5.5) alike.

👉 Read **[CLAUDE.md](CLAUDE.md)**. That is the canonical file: soundness invariants, effort and
delegation policy, and the durable project + mathematical-elementarity rules that used to live
here.

This pointer exists only so Codex sessions, which look for `AGENTS.md` by convention, land in the
right place. Do not add instructions here — put them in `CLAUDE.md`.
