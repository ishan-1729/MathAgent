# AIME 1984 Problem 7

Calibration problem: a nested-recursion functional equation with base case `f(n)=n-3` for
`n >= 1000` and `f(n)=f(f(n+5))` for `n < 1000`; prove `f(84) = 997`.

See `problem.md` for the statement; do not add a full proof here (anti-contamination policy).
